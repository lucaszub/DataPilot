"""Query service — executes sanitized SQL queries against user data sources."""


# TODO: Implement query execution
# - execute_query(data_source, sql) -> list[dict]
# - sanitize_query(sql) -> str (prevent injection)
# - execute_csv_query(file_path, sql) -> list[dict] (via DuckDB)
# - execute_db_query(connection_config, sql) -> list[dict] (via SQLAlchemy)
