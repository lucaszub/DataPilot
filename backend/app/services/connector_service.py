"""Connector service — manages database connections and schema introspection."""


# TODO: Implement connectors
# - test_connection(type, config) -> bool
# - get_schema(type, config) -> dict (tables, columns, types)
# - encrypt_config(config) -> str (Fernet)
# - decrypt_config(encrypted) -> dict (Fernet)
# Supported types (Phase 1): postgresql, mysql, csv
