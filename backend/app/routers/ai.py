from fastapi import APIRouter

router = APIRouter()


# TODO: Implement text-to-SQL endpoint
# POST /query — natural language to SQL query
# Input: { question: str, data_source_id: uuid }
# Output: { sql: str, results: list, chart_suggestion: dict }
