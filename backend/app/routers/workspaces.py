from fastapi import APIRouter

router = APIRouter()


# TODO: Implement CRUD workspaces
# GET / — list workspaces (filtered by tenant_id)
# POST / — create workspace
# GET /{id} — get workspace
# PUT /{id} — update workspace
# DELETE /{id} — delete workspace
