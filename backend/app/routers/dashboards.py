from fastapi import APIRouter

router = APIRouter()


# TODO: Implement dashboard CRUD
# GET / — list dashboards (filtered by tenant_id)
# POST / — create dashboard
# GET /{id} — get dashboard with widgets
# PUT /{id} — update dashboard
# DELETE /{id} — delete dashboard
# POST /{id}/widgets — add widget
# PUT /{id}/widgets/{widget_id} — update widget
# DELETE /{id}/widgets/{widget_id} — delete widget
